# Offline dependencies

Boost 1.82.0 is stored as the extracted source tree in `packages/boost_1_82_0`, so an offline machine can build it directly without reconstructing an archive.

gflags remains stored as `packages/gflags-2.2.2.tar.gz`. Its expected SHA-256 checksum is:

```text
34af2f15cf7367513b352bdcd2493ab14ce43692d2dcd9dfc499492966c64dcf  packages/gflags-2.2.2.tar.gz
```
